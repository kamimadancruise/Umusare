# Server Source

Backend API area: endpoints will be implemented in later steps.

Current entry points:

- `app.js` creates the Express app and mounts `/api` routes.
- `server.js` starts the HTTP server and prepares the database connection.
