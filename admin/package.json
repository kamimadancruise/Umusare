{
  "name": "umusare-admin",
  "version": "0.1.0",
  "private": true,
  "description": "Umusare admin portal.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174",
    "build": "vite build --outDir dist",
    "preview": "vite preview --host 0.0.0.0 --port 4174"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
